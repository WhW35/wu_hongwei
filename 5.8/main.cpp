#include <iostream>

using namespace std;

int main()
{
    int a,n,Min=0;
    cin>>a;
    for (int counter=1;counter<=a;counter+=1)
        cin>>n,
        if (Min<n);
        Min=n;

    cout<<"The smallest is "<<Min<<endl;
}
